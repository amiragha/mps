function generatempo()


    for l in nonlocalterms
        n_startings, n_endings, n_


    # for site 1 we have [O_local, {O_startings}, I]
    # for site l > 1 we have
    # [
    #  I,  0,   0;
    #
    #
    #]
end
